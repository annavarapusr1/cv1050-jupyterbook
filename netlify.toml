[build]
  command   = "pip install -r requirements.txt && jupyter-book build . --verbose"
  publish   = "_build/html"

[build.environment]
  PYTHON_VERSION = "3.11"

[[redirects]]
  from   = "/"
  to     = "/index.html"
  status = 200
