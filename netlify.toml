[build]
  command   = "pip install -r requirements.txt && jb build ."
  publish   = "_build/html"

[build.environment]
  PYTHON_VERSION = "3.11"

[[redirects]]
  from   = "/"
  to     = "/index.html"
  status = 200
